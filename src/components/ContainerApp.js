import styled from 'styled-components';
import {
  COLORS_OBJ,
} from '../StyleGuide';

const ContainerApp = styled.div`
  min-height: 100vh;
  min-width: 100vw;
  background-color: ${COLORS_OBJ.background};
`;

export default ContainerApp;
